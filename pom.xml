<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>dev.catgirlyannick</groupId>
    <artifactId>catclans</artifactId>
    <version>0.1.0-BETA</version>
    <packaging>jar</packaging>

    <name>CatClans</name>
    <description>Configurable clan plugin for {{SERVER_NAME}}.</description>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.build.outputTimestamp>2026-07-31T00:00:00Z</project.build.outputTimestamp>
        <sqlite.version>3.46.1.0</sqlite.version>
        <mysql.version>9.3.0</mysql.version>
        <luckperms-api.version>5.5</luckperms-api.version>
        <placeholderapi.version>2.11.7</placeholderapi.version>
        <vault-api.version>1.7.1</vault-api.version>
    </properties>

    <repositories>
        <repository>
            <id>papermc</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
        <repository>
            <id>placeholderapi</id>
            <url>https://repo.helpch.at/releases/</url>
        </repository>
        <repository>
            <id>jitpack</id>
            <url>https://jitpack.io</url>
        </repository>
    </repositories>

    <dependencies>
        <!-- Paper provides this API at runtime. -->
        <dependency>
            <groupId>io.papermc.paper</groupId>
            <artifactId>paper-api</artifactId>
            <version>1.21-R0.1-SNAPSHOT</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.xerial</groupId>
            <artifactId>sqlite-jdbc</artifactId>
            <version>${sqlite.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <version>${mysql.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>net.luckperms</groupId>
            <artifactId>api</artifactId>
            <version>${luckperms-api.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>me.clip</groupId>
            <artifactId>placeholderapi</artifactId>
            <version>${placeholderapi.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>com.github.MilkBowl</groupId>
            <artifactId>VaultAPI</artifactId>
            <version>${vault-api.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.11.0</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>CatClans-v${project.version}</finalName>

        <!-- Resource filtering injects the project version into plugin.yml. -->
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <proc>none</proc>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.0</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>3.5.0</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireJavaVersion>
                                    <version>[21,22)</version>
                                </requireJavaVersion>
                                <requireMavenVersion>
                                    <version>[3.9,)</version>
                                </requireMavenVersion>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>enforce-plugin-jar-size</id>
                        <phase>verify</phase>
                        <configuration>
                            <target>
                                <condition property="plugin.jar.too.large">
                                    <length
                                            file="${project.build.directory}/${project.build.finalName}.jar"
                                            length="3000000"
                                            when="greater"
                                    />
                                </condition>
                                <fail
                                    if="plugin.jar.too.large"
                                    message="Plugin JAR exceeds the fixed limit of 3,000,000 bytes."
                                />
                            </target>
                        </configuration>
                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>stage-beta-upload</id>
                        <phase>verify</phase>
                        <configuration>
                            <target>
                                <mkdir dir="${project.build.directory}/upload/Latest-Beta" />
                                <copy
                                    file="${project.build.directory}/${project.build.finalName}.jar"
                                    todir="${project.build.directory}/upload/Latest-Beta"
                                />
                                <copy todir="${project.build.directory}/upload/Latest-Beta">
                                    <fileset dir="${project.basedir}/docs">
                                        <include name="INSTALLATION.md" />
                                        <include name="COMMANDS.md" />
                                        <include name="PLACEHOLDERS.md" />
                                        <include name="PERMISSIONS.md" />
                                        <include name="BETA_TEST.md" />
                                        <include name="INTERACTIVECHAT.md" />
                                        <include name="CONFIG_AND_REWARDS_GUIDE.html" />
                                        <include name="DOWNLOAD_DESCRIPTION.md" />
                                    </fileset>
                                </copy>
                                <copy
                                    file="${project.basedir}/BRANDING.md"
                                    todir="${project.build.directory}/upload/Latest-Beta"
                                />
                            </target>
                        </configuration>
                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
